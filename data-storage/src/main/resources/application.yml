server:
  port: 7000
spring:
  profiles:
    include:
      - eureka
      - pvc
      - mongodb
  application:
    name: ${pvc.dataStorage.appName}
  data:
    mongodb:
      auto-index-creation: true